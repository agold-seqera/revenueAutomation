<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Price_USD__c</fullName>
    <description>Total Price converted to USD using locked exchange rate - provides consistent USD reporting for Assets regardless of currency fluctuations</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(Exchange_Rate__c), 
    NULL, 
    "$" + TEXT(ROUND(Total_Price__c / Exchange_Rate__c, 2))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Total Price converted to USD using the locked exchange rate inherited from Opportunity Line Item. Updates automatically when exchange rate or Total Price changes.</inlineHelpText>
    <label>Total Price (USD)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
